
# TODO for get_config_dir():
# Automatically create the configuration directory if it does not exist.
# This improves UX so the user does not need to manually create:
# C:\Users\<user>\AppData\Roaming\claude-code-type-agent\
#
# Example implementation:
# config_dir.mkdir(parents=True, exist_ok=True)
#
# Many CLI tools (Git, Docker, Claude Code) automatically create their
# config directories on first run.
# ===========

=========================================================

4️⃣ Comment for CLI /config Command

Add near CLI commands.
# TODO:
# Implement a `/config` CLI command.
#
# This command should:
# - Open the system config file in the user's default editor
# - Create the config file automatically if it does not exist
#
# Example:
# > /config
#
# Opens:
# C:\Users\<user>\AppData\Roaming\claude-code-type-agent\config.toml
#
# Similar feature exists in tools like Claude Code and Cursor.


name = "openrouter/free"

=========================================
Add near _merge_dicts().
# TODO:
# Document configuration priority order.
#
# Current precedence:
#
# 1. CLI arguments (highest priority)
# 2. Project config (.claude-code-type-agent/config.toml)
# 3. System config (AppData/Roaming/.../config.toml)
# 4. Default values in Config class
#
# This ensures project-specific settings override global defaults.

=========================================================


6️⃣ Comment for Config Validation Improvements

Add near validate().

# TODO:
# Extend configuration validation:
#
# - Validate that the model name is a supported provider/model.
# - Validate that temperature is within safe range.
# - Validate API endpoint format.
#
# This will help catch configuration mistakes early.

===============================================================




TODO:
# IF you need to add seafty polycy or approveval policy type toggle then you do from configurtion system



=======================================================================
✅✅✅✅  TODO: (8:59:04)
Apply Patch which take different edits in one timr amkre edit in one tool call rather tan making multiple edit tool callThis too is known as patch tool call which present in open ai codex 



======================================================================
✅✅✅✅ TODO (In list_dir tool)
Now you csn extnd this tool and give features of recursive file listing  (currentally not supported this)







New todo tasks:
Improve TUI when adding message and show better thinking loader ... 
TUI break when I do resize then alos fix this .
Try to add db layer to save session oriented data and context {if fisible}

















======================== Some new direction ====================

1. In codex cli havae patch apply like if want to edit/changes in file then it not make many tool call for multiple line changes in same file but in my current project that is not implemented.